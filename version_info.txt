# UTF-8
#
# Windows version info resource for Time Aligned.
# Embedded into the exe by PyInstaller via the version_file= argument in
# time_tracker.spec.
#
# Displayed in:
#   - Windows Task Manager (Details tab → Description / Product Name)
#   - File Properties → Details tab
#   - Windows installer dialogs
#
# To update the version, change the four-tuple values below and the
# StringFileInfo fields accordingly.

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers are (major, minor, patch, build) tuples.
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,        # VOS_NT_WINDOWS32
    fileType=0x1,      # VFT_APP
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          # 040904B0 = English (US), Unicode
          '040904B0',
          [
            StringStruct('CompanyName',      ''),
            StringStruct('FileDescription',  'Time Aligned - Time Tracker'),
            StringStruct('FileVersion',      '1.0.0.0'),
            StringStruct('InternalName',     'TimeAligned'),
            StringStruct('LegalCopyright',   ''),
            StringStruct('OriginalFilename', 'TimeAligned.exe'),
            StringStruct('ProductName',      'Time Aligned'),
            StringStruct('ProductVersion',   '1.0.0'),
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct('Translation', [0x0409, 1200])])
  ]
)
